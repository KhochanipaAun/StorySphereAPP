<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/confirm_popup_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#66000000"
    android:visibility="visible">

    <LinearLayout
        android:id="@+id/popup_box"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical"
        android:background="@drawable/bg_dialog_top_rounded"
        android:paddingTop="20dp"
        android:paddingBottom="0dp">

        <!-- ข้อความ -->
        <TextView
            android:id="@+id/tv_popup_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Do you really wish to proceed with deleting this user?"
            android:textStyle="bold"
            android:textSize="16sp"
            android:textColor="@color/darkpurple"
            android:gravity="center"
            android:layout_marginBottom="16dp"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"/>

        <!-- ปุ่มชิดกันแนบล่าง -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:orientation="horizontal"
            android:divider="@null"
            android:showDividers="none"
            android:padding="0dp">

            <Button
                android:id="@+id/btn_popup_no"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:text="No"
                android:textStyle="bold"
                android:textColor="@color/purple1"
                android:background="@drawable/bg_button_bottom_left"
                android:padding="0dp" />

            <Button
                android:id="@+id/btn_popup_yes"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:text="Yes"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:background="@drawable/bg_button_bottom_right"
                android:padding="0dp" />
        </LinearLayout>
    </LinearLayout>
</RelativeLayout>